const name = "rajat"
const countr = 50

const newname = new String('hello')

console.log(newname)

//console.log(name + countr + " value")  dont use this sintax instead go for string interpolation

console.log(`my name is ${name} and repo count is ${countr}`) // 

// go to console and check prototype their